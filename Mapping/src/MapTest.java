import java.util.ArrayList;
import java.util.HashMap;
import java.util.TreeMap;

public class MapTest {

	public static void main(String[] args) 
	{
		//Two types
		
		//maps can hold unique quantities, 
		//like dictionary
		//key : value pair
		//make sure no duplicate
		
		//indexed structure [1, 2, 3, 4, 5, 6, 7, 8, 9] 
		
		SearchHandler s = new SearchHandler();
		System.out.println(s.Search(""));
		
		//TreeMap<String, ArrayList<String>> tree = new TreeMap<String, ArrayList<String>>(); //ordered - slower
		
		//word : meaning
		//key -> mapped to value
		//domain : range in math
		// domain mapped to range
		
		//Write a dictionary
		//can add words to the dictionary with their meanings
		//we cab search for a word with its meaning
		//we can search for a meaning given its word
		//need to store the dictionary in a file as we add ore words
		//need to extract new words from any given file to be added to our dictionary
		//find the meaning of a word - perform a online lookup or user defined
		
		//Use binary search to get meaning with a word
		//
		//Word word = new Word();
		
		//HashMap hash = new HashMap(); //no order - faster
	}

}
